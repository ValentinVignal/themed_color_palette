builders:
  # infoGeneratorBuilder:
  #   import: "package:theme_color_palette/theme_color_palette.dart"
  #   builder_factories: ["infoGeneratorBuilder"] # Names of the functions
  #   build_extensions: { ".dart": [".info.g.part"] }
  #   auto_apply: dependents
  #   build_to: cache
  #   applies_builders: ["source_gen|combining_builder"]
  # classExtrasGeneratorBuilder:
  #   import: "package:theme_color_palette/theme_color_palette.dart"
  #   builder_factories: ["classExtrasGeneratorBuilder"] # Names of the functions
  #   build_extensions: { ".dart": [".fields.g.part"] }
  #   auto_apply: dependents
  #   build_to: cache
  #   applies_builders: ["source_gen|combining_builder"]
  themeColorPaletteGeneratorBuilder:
    import: "package:theme_color_palette/theme_color_palette.dart"
    builder_factories: ["themeColorPaletteGeneratorBuilder"]
    build_extensions: { ".theme.yaml": [".theme.g.dart"] }
    build_to: source
    auto_apply: dependents
